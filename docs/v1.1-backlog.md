# XICO · v1.1+ backlog

Everything we deferred during the v1 / TestFlight push. Sized S/M/L, ordered by user-impact. **Pick from the top.**

Last updated 2026-05-14 night, after TestFlight build #10 submission to Apple.

## 🌱 Open question that affects v1.1 scope

**Is "XICO" the final name?** Mauricio said tonight he's not sure. Until decided:

- Don't register `xico.app` domain (risk: spent on name we abandon)
- Don't ship the public landing page to a permanent URL
- Don't pay for ElevenLabs Starter under the XICO Apple Developer record (the API key belongs to Mauricio's account, but the brand voice setup is XICO-specific)
- Don't print press kits or send to journalists
- Don't expand external TestFlight pool beyond 1-2 close people

**Decision deadline**: before the second Ruta drops (Sunday Week 20, 2026-05-24). Either commit to XICO or rename before more user equity accumulates around it.

If the name changes: the rename surface is ~10 files (app.json display name, EAS project rename, App Store Connect rename, brand docs, copy in despachos, landing page hero, social handles). About 4-6 hours of focused work. Easier to do at <5 testers than at >50.

---

## Priority 1 · launch-blocking for public App Store (NOT internal TestFlight)

| # | Item | Size | Notes |
|---|---|---|---|
| 1 | Decide on the final name | S | See above. Either confirm XICO or pick alternative + rename. |
| 2 | Register the domain | S | If keeping XICO → register `xico.app`. Cloudflare Registrar (~$10/yr). DNS to Vercel after deploy. |
| 3 | Deploy the landing page | S | The `artifacts/xico-web` Next.js site is committed and ready. 3 commands: `vercel login`, `vercel link`, `vercel --prod`. |
| 4 | App Privacy questionnaire in App Store Connect | M | Apple requires before public submission. Answers drafted in `docs/brand/app-store-listing.md` (Privacy answers section). Just fill the web form. |
| 5 | App Store screenshots (1290×2796) | M | Need ≥3 captured from the actual TestFlight build. The 5-shot brief is in `app-store-listing.md`. Probably 30-60 min in iOS Simulator with the script. |
| 6 | Apple Beta App Review submission for external testers | S | Requires #4 + #5. Apple's review is 24-48h. |

## Priority 2 · meaningful product completeness (v1.1 features)

| # | Item | Size | Why it matters |
|---|---|---|---|
| 7 | ElevenLabs paid tier ($5/mo Starter) | S | Unblocks the audio narrator — already fully integrated, just blocked by free-tier abuse detector. Single biggest editorial feature un-shipped. |
| 8 | Real photographs per Ruta stop | L | Currently the Stop hero is editorial typography only. v1.1 could add a hero photo to each. Needs photographer or rights-cleared imagery for the 5 inaugural stops. |
| 9 | Push notifications + handler | M | "Sunday Ruta drop" notification (Sunday 9 AM). Requires Apple Push Notification certificate + Supabase Edge Function or external worker. The copy is drafted in `docs/brand/copy-library.md`. |
| 10 | Mapa tab · real Leaflet WebView | M | Currently stubbed to "Madrid · próximamente." Real implementation = WebView + Leaflet HTML + 5 numbered rumbo-colored markers. ~3-4 hours. |
| 11 | Roseton per-week archive (Tu Códice) | L | Currently the Rosetón shows only this week. Architectural: each completed Ruta becomes a permanent rosetón in an archive. Manifesto-aligned ("the places stay"). |
| 12 | Editor rotation infrastructure | M | Manifesto names 4 editors (María Vázquez, Sofía Niño de Rivera, Damián Ortega, Andrés Felipe Solano). Currently only María's voice in seed. Adding new editors needs an admin path or per-week seed file. |

## Priority 3 · auth/UX polish

| # | Item | Size | Notes |
|---|---|---|---|
| 13 | Magic-link resend button + 60s cooldown | S | Drafted in earlier P1 plan as deferred. Common user pain. |
| 14 | Expired magic-link screen | S | Currently silent fail. Need `app/auth/expired.tsx` + setSession error catch in `_layout.tsx`. |
| 15 | Auth error clarity (parse Supabase error codes) | S | Currently shows "no pudimos enviar el enlace" — be specific (rate-limit vs network vs invalid email). |
| 16 | Onboarding back button mid-flow | S | Currently one-way; AsyncStorage preserves state but back is missing. |
| 17 | Cultura/Mexico-Ahora final typography sweep | M | Most labels already lifted but ~15 rgba gradient/decorative colors remain. P2 cosmetic. |

## Priority 4 · operational hardening

| # | Item | Size | Notes |
|---|---|---|---|
| 18 | Rotate the three compromised credentials | S | Apple ID password, Supabase PAT, ElevenLabs API key — all in chat transcripts. Mauricio declined this session; reassess at any quiet moment. |
| 19 | Service-role JWT separate from anon for elevated server ops | M | Currently the API server uses anon key for everything; this works because RLS is disabled. Long-term: re-enable RLS + use service-role JWT for elevated paths + per-user JWT for user-scoped paths. Defense in depth. |
| 20 | seed.sql alignment with seed-ruta-rumbos.ts | S | seed.sql still has the stale "La Tilica" + wrong addresses. Production is correct via the override seed, but a fresh DB rebuild from seed.sql alone would produce stale data. Hygiene. |
| 21 | Geo radius widen path (50m → 100m fallback?) | S | If real-world walking reveals 50m is too tight in dense Madrid, widen with a config flag. Backend-only change. |
| 22 | madrid_pulse admin path (replace hand-written seed) | M | Currently 30 days hand-typed in the seed file. v1.5: María Vázquez writes daily via a tiny admin UI or Supabase row insert. |

## Priority 5 · marketing presence

| # | Item | Size | Notes |
|---|---|---|---|
| 23 | Instagram @xico.madrid | S | User confirmed account doesn't exist yet. Bio + 3-5 launch posts ready to draft when the handle is claimed. |
| 24 | Press contact list | M | Madrid culture journalists, expat-Mexico community publications, Spain food/culture media. Build before any outreach. |
| 25 | Sello Copil partnership content | M | App already references "Sello Copil restaurantes" in copy. Real partnership = directory of certified Mexican restaurants in Madrid + curator decisions. |
| 26 | Substack / newsletter for despachos archive | L | Editorial pieces have permanent value beyond the app. A weekly Substack of the Ruta despachos as long-form pieces is the obvious distribution channel. |

## Priority 6 · architecture (after v1.5)

| # | Item | Size | Notes |
|---|---|---|---|
| 27 | iOS Live Activity for active Ruta | L | When a user starts walking, a Live Activity in Dynamic Island shows progress: "Caminando · 2/5 sellos · Casa de México." |
| 28 | iOS Widget | L | Home screen widget showing the week's Despacho. |
| 29 | Apple Watch companion | XL | Watch face complication for the active Ruta + sello earn haptic on arrival. |
| 30 | Localization · English | L | Strings prep + translation pass. The press kit + listing already have English variants drafted. |
| 31 | Localization · Catalan | L | Madrid + Mexican users in Catalonia. |
| 32 | Android version | XL | Material Design 3 adaptation of the editorial system. Same backend. ~4-6 weeks. |

## Out of scope (NEVER, by manifesto)

- Streaks
- Daily login rewards
- Push notifications that pressure ("don't forget your sello!")
- Leaderboards
- XP / points UI
- Mascot logic / cartoon characters
- Folkloric design tropes (sombreros, mariachi clip-art, generic "Mexico" iconography)
- Tourist-guide framing ("top 10 Mexican restaurants in Madrid")
- AI-generated despachos · the editor's name on each piece IS the brand
