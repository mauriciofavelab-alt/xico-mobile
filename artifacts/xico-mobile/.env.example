# Variables de entorno para desarrollo local (fuera de Replit)
# Copia este archivo como .env.local y ajusta los valores

# Dominio base del servidor API (sin protocolo)
# Dentro de Replit: asignado automáticamente ($REPLIT_DEV_DOMAIN)
# Fuera de Replit: usa tu IP local o el dominio donde corre la API
EXPO_PUBLIC_DOMAIN=localhost:8080

# Supabase project · required for auth, sellos, saved articles, profile,
# editor letters, and most remote data. Missing values degrade gracefully
# (see constants/supabase.ts) — the app still mounts, but auth + remote
# calls reject with a clear "Supabase not configured" error.
# Find these at https://supabase.com/dashboard/project/_/settings/api
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Google Sign-In (iOS) · required for the "Continuar con Google" auth path.
# Find this at Google Cloud Console → APIs & Services → Credentials →
# OAuth 2.0 Client IDs → iOS application (create one if needed). Also
# update the iosUrlScheme in app.json plugins.@react-native-google-signin
# entry · it's the *reversed* client ID (com.googleusercontent.apps.NNN…).
# When unset/placeholder, the Google button shows "Configuración pendiente"
# soft-notice and the user can still use Apple + magic-link.
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-google-ios-client-id.apps.googleusercontent.com

# Legal URLs · displayed in the auth screen footer. If unset, the bracketed
# words ("Términos", "Privacidad") render as disabled labels instead of
# Pressables. Update both before App Store submission.
EXPO_PUBLIC_TERMS_URL=
EXPO_PUBLIC_PRIVACY_URL=

# Solo necesario dentro de Replit — ignorar fuera
# EXPO_PACKAGER_PROXY_URL=
# REACT_NATIVE_PACKAGER_HOSTNAME=
# REPL_ID=
